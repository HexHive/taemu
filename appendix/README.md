# Artifact appendix

LaTeX source of the artifact appendix submitted with the paper. It is a
document, not code: no experiment reads anything here.

    paper.tex         the appendix
    bibliography.bib  its references
    IEEEtran.cls      the IEEE class file (third party, see ../NOTICE)
    Makefile          `make` builds paper.pdf; the build output is gitignored

The appendix summarises what [`../README.md`](../README.md) documents in full.
